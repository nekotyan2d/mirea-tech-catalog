# Каталог компьютерной техники

Консольное приложение для учета товаров компьютерной техники
на складе и оформления заказов на них.

## Назначение

Приложение ведет каталог товаров (ноутбуки, мониторы, периферия
и т. д.), отслеживает остатки на складе и позволяет оформлять
и отменять заказы на товары с проверкой доступного количества.

## Целевая аудитория

Небольшой магазин или склад компьютерной техники, которому нужен
простой инструмент учета товаров и заказов.

## Предметная область

Учет товаров компьютерной техники: категории, производители, цены,
остатки на складе и заказы клиентов на эти товары.

## Основные сущности

- товар (id, название, id категории, id производителя, цена, количество);
- категория (id, название);
- производитель (id, название, страна);
- заказ (id, товар, количество, статус).

## Возможности

- просмотр и поиск товаров по названию;
- фильтрация по категории и по диапазону цен;
- сортировка товаров по цене или названию;
- проверка наличия товара на складе;
- оформление и отмена заказов с проверкой доступности;
- статистика по каталогу (остатки, стоимость, средняя цена);
- добавление новых товаров в каталог.

## Структура проекта

```
tech_catalog/
├── README.md
├── requirements.txt
├── setup.cfg
├── main.py             — точка запуска
├── products.py         — работа с товарами
├── categories.py       — работа с категориями
├── manufacturers.py    — работа с производителями
├── orders.py           — работа с заказами
├── storage.py          — сохранение и загрузка данных
├── utils.py            — вспомогательные функции ввода
├── data/
│   ├── products.json      — данные о товарах
│   ├── categories.json    — данные о категориях
│   ├── manufacturers.json — данные о производителях
│   └── orders.json        — данные о заказах
└── tests/
    ├── test_products.py
    ├── test_categories.py
    ├── test_manufacturers.py
    └── test_orders.py
```

## Формат хранения данных

Данные о товарах и заказах хранятся в формате JSON в каталоге
`data/`. При запуске программа загружает данные из файлов,
при выходе из программы (пункт меню «0. Выход») — сохраняет их.

## Требования

- Python 3.10+;
- pytest;
- flake8.

Установка зависимостей для разработки:

```
pip install -r requirements.txt
```

## Запуск программы

```
python main.py
```

## Запуск тестов

```
pytest
```

## Проверка качества кода

```
flake8
```

## План развития

На следующих этапах планируется:

- переход на объектно-ориентированную модель (классы Product, Order);
- разработка веб-приложения на Django;
- подключение базы данных;
- реализация пользователей и ролей (менеджер склада, клиент);
- разработка REST API;
- контейнеризация приложения и настройка CI/CD.
